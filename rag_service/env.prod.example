# ============================
# RAG Service 环境变量配置（生产环境）
# 使用方法：复制此文件为 .env.prod，然后启动时设置 ENV=prod
# ============================

# ============================
# 服务配置
# ============================
SERVICE_HOST=0.0.0.0
SERVICE_PORT=8001
DEBUG=false

# ============================
# MySQL 数据库配置（生产环境）
# 用于读取智谱AI API key
# 请在实际部署前修改为实际的数据库配置
# ============================
DB_HOST=mysql
DB_PORT=3306
DB_USER=root
DB_PASSWORD=change_this_in_production
DB_NAME=soulmate_db

# ============================
# 智谱AI配置（可选，优先从数据库读取）
# 如果数据库读取失败，将使用以下环境变量作为后备
# ============================
ZHIPU_API_KEY=your_zhipu_api_key
ZHIPU_API_BASE=https://open.bigmodel.cn/api/paas/v4
EMBEDDING_MODEL=embedding-2

# ============================
# ChromaDB 配置（后续使用）
# ============================
CHROMA_PERSIST_PATH=./data/chroma

