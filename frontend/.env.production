# 前端访问后端 API 的基础路径（生产环境）
VITE_BASE_API=/api

# 在容器/预览模式下需要代理时的后端地址（正常生产由 Nginx 负责，这里只是备用）
VITE_PROXY_TARGET=http://backend:8080

# 生产环境关闭 Mock
VITE_USE_MOCK=false

# 应用标题
VITE_APP_TITLE=SoulMate 心伴

# 生产环境标识
NODE_ENV=production
